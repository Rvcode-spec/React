Qu..What are React components?

Ans..:- React components are the building blocks of a React application. They can be functional (stateless) or class-based (stateful) and are responsible for rendering the UI.